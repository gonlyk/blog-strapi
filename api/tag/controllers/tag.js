'use strict';

/**
 * Read the documentation (https://strapi.io/documentation/developer-docs/latest/development/backend-customization.html#core-controllers)
 * to customize this controller
 */

module.exports = {
  // async find(ctx) {
  //   let entities;
  //   if (ctx.query._q) {
  //     entities = await strapi.services.tag.search(ctx.query);
  //   } else {
  //     entities = await strapi.services.tag.find(ctx.query);
  //   }

  //   // return entities.map(entity => sanitizeEntity(entity, { model: strapi.models.restaurant }));
  //   return entities;
  // },
  // async findOne(ctx) {
  //   const { id } = ctx.params;
  //   const entity = await strapi.services.tag.findOne({ id });
  //   // return sanitizeEntity(entity, { model: strapi.models.blogPost});
  //   return entity;
  // }
};
